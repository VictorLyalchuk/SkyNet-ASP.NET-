﻿@using Microsoft.AspNetCore.Identity;
@using SkyNet.Core.DTOs.User;
@using SkyNet.Core.Entities.User;
@inject UserManager<AppUser> UserManager

@{
    var user = await UserManager.GetUserAsync(User);
    var email = await UserManager.GetEmailAsync(user);
}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Delete a user</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="#">Home</a></li>
                        <li class="breadcrumb-item active">Users</li>
                    </ol>
                </div>
            </div>
        </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header d-flex justify-content-center">
                            <h3 class="card-title">Do you want to delete a user?</h3>
                        </div>
                        <div class="card-body d-flex justify-content-center">
                            <div class="col-2 ">
@*                                <form asp-controller="Dashboard" asp-action="Delete" method="post">
                                    <input type="hidden" name="ID" value="@Model.ID" />
                                    <button type="submit" class="btn btn-block btn-warning btn-sm">Confirm</button>
                                </form>*@


                                <form id="deleteForm" asp-controller="Dashboard" asp-action="Delete" method="post">
                                    <input type="hidden" name="ID" value="@Model.ID" />
                                </form>
                                 <a href="javascript:void(0);" onclick="deleteConfirmation();" class="btn btn-block btn-warning btn-sm" style="text-decoration: none; color: white;">Confirm</a>
@*                                <a asp-controller="Dashboard" asp-action="Delete" class="btn btn-block btn-warning btn-sm">Confirm</a>
*@                                <a asp-controller="Dashboard" asp-action="GetAll" class="btn btn-block btn-danger btn-sm">Cancel</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /.col -->
            <!-- /.row -->
        </div>
        <!-- /.container-fluid -->
    </section>
</div>
<!-- /.content -->

<script>
    function deleteConfirmation() {
        //var confirmed = confirm("Are you sure you want to delete this user?");
        //if (confirmed) {
            document.getElementById("deleteForm").submit();
        //}
    }
</script>